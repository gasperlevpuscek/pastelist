<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/atTaskName"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="@string/task_name"
        android:inputType="text"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:autofillHints="Task name"/>

    <EditText
        android:id="@+id/atTaskDescription"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="@string/task_description"
        android:inputType="textMultiLine"
        android:layout_marginTop="8dp"
        android:autofillHints="Task description"/>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/linearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp">

        <!-- Date -->
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/atTaskPickDate"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="6dp"
            android:clickable="true"
            android:focusable="false"
            android:inputType="none"
            android:hint="@string/atTaskDate"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/atTaskPickTime"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Time -->
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/atTaskPickTime"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_marginEnd="12dp"
            android:clickable="true"
            android:focusable="false"
            android:inputType="none"
            android:hint="@string/atTaskTime"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/atTaskAdd"
            app:layout_constraintStart_toEndOf="@id/atTaskPickDate" />

        <ImageButton
            android:id="@+id/atTaskAdd"
            android:layout_width="50dp"
            android:layout_height="48dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/add_task"
            android:src="@android:drawable/ic_media_play"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/atTaskPickTime"
            app:layout_constraintTop_toTopOf="@+id/atTaskPickDate" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</LinearLayout>